orders = ["hitesh", "suresh", "ramesh"]
# list → ordered, mutable collection

for order in orders:
    # direct iteration over list elements
    # no index used (Pythonic approach)

    print(f"order is ready for {order}")
